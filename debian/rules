#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

LDFLAGS += -Wl,-z,defs -Wl,--as-needed

%:
	dh  $@

override_dh_auto_test:

override_dh_makeshlibs:
	dh_makeshlibs -V 'libindicator0 (>= 0.3.6)'
